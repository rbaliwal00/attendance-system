import React from 'react';
import Subjects from '../teacher/Subjects';

const Dashboard = () => {
    return (
        <div>
            <Subjects />
        </div>
    );
};

export default Dashboard;